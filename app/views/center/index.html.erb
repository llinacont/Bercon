<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />

  <!-- Always force latest IE rendering engine (even in intranet) & Chrome Frame 
       Remove this if you use the .htaccess -->
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />

  <title>index.html</title>
  <meta name="description" content="" />
  <meta name="author" content="Carlos" />

  <meta name="viewport" content="width=device-width; initial-scale=1.0" />

  <!-- Replace favicon.ico & apple-touch-icon.png in the root of your domain and delete these references -->
  <link rel="shortcut icon" href="/favicon.ico" />
  <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
</head>

<body>
  <div>
    <header>
      <h3>Tu empresa:</h3>
    </header>
   
	<div class="container-fluid">
  		<div class="row-fluid">
    		<div class="span2">
      		<!--Sidebar content-->
      		<ul class="nav nav-list">
  				
	  			<li class="nav-header">Productos</li>
	  				<li><%= link_to 'Crear', demands_path, :style => "color:#049cbd"%></li>
	  				<li><%= link_to 'Editar', demands_path, :style => "color:#049cbd" %></li>
	  				<li><%= link_to 'Mostrar', demands_path, :style => "color:#049cbd" %></li>
  				<li class="nav-header">Clientes</li>
  					<li><%= link_to 'Crear', demands_path, :style => "color:#049cbd" %></li>
  					<li><%= link_to 'Editar', demands_path, :style => "color:#049cbd" %></li>
  					<li><%= link_to 'Mostrar', demands_path, :style => "color:#049cbd" %></li>
  				<li class="nav-header">Pedidos</li>
  					<li><%= link_to 'Crear', demands_path, :style => "color:#049cbd" %></li>
  					<li><%= link_to 'Editar', demands_path, :style => "color:#049cbd" %></li>
  					<li><%= link_to 'Mostrar', demands_path, :style => "color:#049cbd" %></li>
  				<li class="divider"></li>
  				<li><%= link_to 'Editar usuario', edit_user_registration_path %></li>
  				
			</ul>
      		
    		</div>
    	<div class="span10">
      		
      		<div class="row">
 			 <div class="span6">
 			 	
 			 	<table class="table table-striped">
  					<tr>
    					<th>Nombre</th>
    					<th>Precio</th>
    				</tr>
  
					<% @products = Product.all %>
					<% @products.each do |product| %>
  					<tr>
    					<td><%= product.title %></td>
    					<td><%= product.price %></td>
  					</tr>
					<% end %>
				</table>
 			 	
 			 </div>
  			 <div class="span6">
  			 	<table class="table table-striped">
  					<tr>
    					<th>CIF</th>
    					<th>Nombre</th>
    				</tr>
  
					<% @clients = Client.all %>
					<% @clients.each do |client| %>
  					<tr>
  						<td><%= client.cif %></td>
    					<td><%= client.name %></td>
  					</tr>
					<% end %>
				</table>
  			 </div>
			</div>
			
			<div class="row">
 			 <table class="table table-striped">
  					<tr>
    					<th>Fecha</th>
    					<th>Cliente</th>
    				</tr>
  
					<% @demands = Demand.all %>
					<% @demands.each do |demand| %>
  					<tr>
    					<td><%= localize(demand.created_at, :format => :long) %></td>
    					<td><%= Client.find(1).name %></td>
  					</tr>
					<% end %>
				</table>
			</div>
			
    	</div>
  	</div>
  </div>

    <footer>
    </footer>
    
  </div>
</body>
</html>
